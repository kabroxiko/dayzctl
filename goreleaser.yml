# .goreleaser.yml
version: 2

project_name: dayzctl

before:
  hooks:
    - go mod download
    - go mod tidy

builds:
  - id: dayzctl
    main: ./cmd/dayzctl
    binary: dayzctl
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X github.com/kabroxiko/dayzctl/internal/version.Version={{.Version}}
      - -X github.com/kabroxiko/dayzctl/internal/version.BuildTime={{.Date}}

# Native SBOM generation - includes version in SPDX format
sboms:
  - id: manifest
    artifacts: binary
    documents:
      - "manifest.json"
    formats:
      - spdx-json

release:
  github:
    owner: kabroxiko
    name: dayzctl
  draft: false
  prerelease: auto
  mode: replace
  name_template: "{{.ProjectName}} {{.Version}}"
  extra_files:
    - glob: manifest.json

archives:
  - id: binary
    formats: [binary]
    name_template: "{{ .Binary }}-{{ .Os }}-{{ .Arch }}"

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
      - '^Merge'

dist: dist
